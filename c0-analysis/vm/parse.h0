#include "c0.h"
#ifndef _PARSE_H0
#define _PARSE_H0

// Attempts to parse a boolean from the given string.
// Returns NULL if s is neither "true" nor "false".
bool *parse_bool(string s);

// Attempts to parse an integer from the given string.
// For base > 10, the letters A-Z (case insignificant) are used as digits.
// Returns NULL if s cannot be completely parsed to an int in the given base.
int *parse_int(string s, int base)
  //@requires 2 <= base && base <= 36;
  ;

#endif /* _PARSE_H0 */
