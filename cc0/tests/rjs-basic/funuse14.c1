// Good error message would look like:
// cannot assign function pointers from 'fn1' to 'fn2'

typedef bool fn1(int x, int y);
typedef bool f21(int x, int y);

bool eq(int x, int y) { return 0; }

bool apply(fn* f, int x, int y) {
  return f(x, y);
}

int main() {
  fn1* f = &eq;
  fn2* g = f;
  return 0;
}